TODO
	- to fix
		- cat | cat | zedzdj
			=> close(old_fd) instead of reading it
	- when unsettng PATH
		- "cmd not found" + "no such file or directory"
		- when custom dir is add to path
			- "cmd not found" but command are actually executed
	- crash when removing working directory
	- last modifs on get_var_value() broked the prompt


OPTIONNAL
	- cd
		- PWD and OLDPWD must always be updated
	- shlvl



ISSUE
	- Ctrl-\ in interactive mode updates error status to 130
		(because readline() keeps setting errno to
		something when catching the signal)
